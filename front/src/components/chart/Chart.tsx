import React, { FC } from 'react';
import Title from '../Title';

const Chart: FC = (): JSX.Element => {
	return (
		<React.Fragment>
			<Title>Today</Title>
		</React.Fragment>
	);
};

export default Chart;
